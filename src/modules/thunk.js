export function thunkAction() {
  return async(dispatch, getState) => {
    // fetching
    // dispatch();

    // const response = await window.fetch("/");
    // await response.json();

    // fetched
    // dispatch();
    // save
    // dispatch();
  }
}
